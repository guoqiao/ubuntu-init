Ubuntu/Mint的上手配置
由于喜新厌旧的本性,上周末我终于忍不住给笔记本装上了刚出几天的Linux Mint 14,用了2年多的Ubuntu惨遭格式化.
放弃Ubuntu的主要原因是觉得它太丑了.以前我没的选,而现在有了Mint.
实不相瞒, 我早就被它小清新的靓丽外观吸引，倾心已久。而Ubuntu万年不变的紫色界面确实太倒人胃口。
听着怪怪的...
fakedomain  192.168.15.220  spig-utils

change source

sudo apt-get install libnss3-dev
install chrome

install vim
sudo apt-get install gnome-shell

ime

language support
select ibus
add/remove language
键盘，preference,input method


change workspace to 2
??something wrong: when switch to w2, panels gone

??
Enter password for keyring 'Default' to unlock

hg
python.h
install python-dev

config hg:
[ui]
username = guoqiao <guoqiao@insigma.com.cn>

[auth]
m.prefix = http://iot-hg.insigma.com.cn/
m.username = guoqiao
m.password = ******

install git
ssh-keygen cp id_rsa.pub to github
git clone git@github.com:guoqiao/vimconf.git

config ssh

install setuptools,pip
config

pip install django

sudo apt-get install nautilus-open-terminal

email, thunderbird
auto detect

EnvironmentError: mysql_config not found
sudo apt-get install libmysqld-dev libmysqlclient-dev

pygraphviz
sudo apt-get install graphviz-dev


fatal error: cgraph.h: No such file or directory # a mistake

ldap support
sudo apt-get install libssl-dev libldap2-dev libsasl2-dev

pip install supervisor ERR
use apt-get

install Lightning for thunderbird

